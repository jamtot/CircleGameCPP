#include "Test.h"

int main() {
	int x, y;
	
	std::cout << "Give me the first number to add: ";
	std::cin >> x;
	std::cout << "Now, give me a number to add to " << x << ": ";
	std::cin >> y;
	Test t(x,y);
}
